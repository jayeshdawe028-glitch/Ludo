services:
  ludocord:
    build: .
    container_name: aarohi
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - aarohi_data:/data

volumes:
  aarohi_data:
